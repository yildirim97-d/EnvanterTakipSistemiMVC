@using sirket.Models.Entity;
@model zimmet
@{

    List<cihaz> cihazModel = (List<cihaz>)ViewBag.cihaz;
    List<kullanici> kullaniciModel = (List<kullanici>)ViewBag.kullanici;
    List<tel_marka> tel_markaModel = (List<tel_marka>)ViewBag.tel_marka;
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    <h4>zimmet</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <label>Cihaz Adı</label>
    @Html.DropDownListFor(model => model.cihaz_id, (IEnumerable<SelectListItem>)ViewBag.CihazIsim, new { @class = "form-control" })
    @*<div class="form-group row">

            <div class="col-sm-10">
                <select class="form-control" name="cihaz_id">
                    @foreach (cihaz cihazlar in cihazModel)
                    {
                        <option selected="@Model.cihaz_id" value="@cihazlar.cihaz_id">@(cihazlar.cihaz_ad)</option>
                    }
                </select>
            </div>
        </div>*@

    <br />
    <label>Kullanıcı Adı</label>
    @Html.DropDownListFor(model => model.kullanici_id, (IEnumerable<SelectListItem>)ViewBag.KullaniciIsim, new { @class = "form-control" })
    @*<div class="form-group row">

            <div class="col-sm-10">
                <select class="form-control" name="kullanici_id">
                    @foreach (kullanici kullanicilar in kullaniciModel)
                    {
                        <option selected="" value="@kullanicilar.kullanici_id">@(kullanicilar.kullanici_adi)</option>
                    }
                </select>
            </div>
        </div>*@
    <br />
    <label>Cep Telefonu Model</label>
    @Html.DropDownListFor(model => model.telmarka_id, (IEnumerable<SelectListItem>)ViewBag.TelMarkaIsim, new { @class = "form-control" })
    @*<div class="form-group row">

            <div class="col-sm-10">
                <select class="form-control" name="telmarka_id">
                    @foreach (tel_marka markalar in tel_markaModel)
                    {
                        <option selected="@Model.telmarka_id" value="@markalar.telmarka_id">@(markalar.telmarka_adi)</option>
                    }
                </select>
            </div>
        </div>*@




    <!--<div class="form-group">-->
    @*@Html.LabelFor(model => model.ceptelefonu_model, htmlAttributes: new { @class = "control-label col-md-2" })
        <!--<div class="col-md-10">
                @Html.EditorFor(model => model.ceptelefonu_model, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.ceptelefonu_model, "", new { @class = "text-danger" })
            </div>
        </div>-->
    *@
    <label>Zimmetleme Tarih</label>
    <div class="form-group">

        @*@Html.LabelFor(model => model.zimmet_tarih, htmlAttributes: new { @class = "control-label col-md-2" })*@
        <div class="col-md-10">
            @Html.EditorFor(model => model.zimmet_tarih, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.zimmet_tarih, "", new { @class = "text-danger" })
        </div>
    </div>

    @Html.HiddenFor(model => model.zimmet_id)

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
